# Copy to .env, fill in your key locally, then use uv run --env-file .env.
# See README.md: API key setup. Environment files are not auto-loaded.
TYPESAFE_API_KEY=
# Optional: only needed for the OpenRouter teacher via --teacher-model.
OPENROUTER_API_KEY=
